<section class="admin-panel">
  <div class="admin-panel__head">
    <h1 class="admin-panel__title"><%= step ? 'Sửa bước quy trình' : 'Thêm bước quy trình' %></h1>
    <hr class="admin-panel__rule">
  </div>

  <form action="<%= action %>" method="POST" enctype="multipart/form-data" class="admin-form">

    <div class="admin-form__row admin-form__row--half">
      <div>
        <label class="admin-form__label">Số bước <span>*</span></label>
        <input type="number" name="step_number" class="admin-form__input" min="1" max="20" required
               value="<%= step ? step.step_number : 1 %>">
      </div>
      <div>
        <label class="admin-form__label">Thứ tự hiển thị</label>
        <input type="number" name="display_order" class="admin-form__input" min="0"
               value="<%= step ? step.display_order : 0 %>">
      </div>
    </div>

    <div class="admin-form__row">
      <label class="admin-form__label">Tiêu đề bước <span>*</span></label>
      <input type="text" name="title" class="admin-form__input" required
             value="<%= step ? step.title : '' %>" placeholder="VD: Tư vấn khách hàng">
    </div>

    <div class="admin-form__row">
      <label class="admin-form__label">Mô tả</label>
      <textarea name="description" class="admin-form__input admin-form__textarea" rows="3"
                placeholder="Mô tả ngắn về bước này"><%= step ? step.description : '' %></textarea>
    </div>

    <div class="admin-form__row">
      <label class="admin-form__label">Hình ảnh minh họa</label>
      <div class="admin-upload-area" id="uploadArea">
        <% if (step && step.image_path) { %>
          <img src="<%= step.image_path %>" class="admin-upload-preview" id="imgPreview">
        <% } else { %>
          <img src="" class="admin-upload-preview" id="imgPreview" style="display:none">
        <% } %>
        <div class="admin-upload-placeholder" id="uploadPlaceholder" style="<%= step && step.image_path ? 'display:none' : '' %>">
          <svg viewBox="0 0 24 24" fill="currentColor"><path d="M19.35 10.04A7.49 7.49 0 0012 4C9.11 4 6.6 5.64 5.35 8.04A5.994 5.994 0 000 14c0 3.31 2.69 6 6 6h13c2.76 0 5-2.24 5-5 0-2.64-2.05-4.78-4.65-4.96zM14 13v4h-4v-4H7l5-5 5 5h-3z"/></svg>
          <p>Kéo thả hoặc <span>chọn ảnh</span></p>
        </div>
        <input type="file" name="image" id="fileInput" accept="image/*" style="display:none">
      </div>
    </div>

    <div class="admin-form__row admin-form__row--half">
      <div>
        <label class="admin-form__label">Màu cube</label>
        <div class="process-color-picker">
          <% CUBE_COLORS.forEach(function(c) { %>
          <label class="process-color-option">
            <input type="radio" name="color" value="<%= c %>" <%= step && step.color === c ? 'checked' : (!step && c === 'blue' ? 'checked' : '') %>>
            <span class="process-cube-mini process-cube--<%= c %>"></span>
          </label>
          <% }); %>
        </div>
      </div>
      <div>
        <label class="admin-form__label">Trạng thái</label>
        <select name="is_active" class="admin-form__input">
          <option value="1" <%= !step || step.is_active ? 'selected' : '' %>>Hiển thị</option>
          <option value="0" <%= step && !step.is_active ? 'selected' : '' %>>Ẩn</option>
        </select>
      </div>
    </div>

    <div class="admin-form__actions">
      <button type="submit" class="admin-btn admin-btn--green">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z"/></svg>
        Lưu
      </button>
      <a href="/admin/pages/process" class="admin-btn admin-btn--gray">Hủy</a>
    </div>
  </form>
</section>

<script>
const uploadArea = document.getElementById('uploadArea');
const fileInput  = document.getElementById('fileInput');
const imgPreview = document.getElementById('imgPreview');
const placeholder = document.getElementById('uploadPlaceholder');
uploadArea.addEventListener('click', () => fileInput.click());
uploadArea.addEventListener('dragover', e => { e.preventDefault(); uploadArea.classList.add('drag-over'); });
uploadArea.addEventListener('dragleave', () => uploadArea.classList.remove('drag-over'));
uploadArea.addEventListener('drop', e => { e.preventDefault(); uploadArea.classList.remove('drag-over'); if (e.dataTransfer.files[0]) preview(e.dataTransfer.files[0]); });
fileInput.addEventListener('change', function() { if (this.files[0]) preview(this.files[0]); });
function preview(file) {
  const reader = new FileReader();
  reader.onload = e => { imgPreview.src = e.target.result; imgPreview.style.display = 'block'; placeholder.style.display = 'none'; };
  reader.readAsDataURL(file);
  const dt = new DataTransfer(); dt.items.add(file); fileInput.files = dt.files;
}
</script>
